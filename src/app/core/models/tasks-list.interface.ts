export interface TasksList {
    id: number;
    title: string;
    description: string;
    assignedDate: string;
    dueDate: string;
    taskStatus: string;
    taskType: string;
    source: string;
}